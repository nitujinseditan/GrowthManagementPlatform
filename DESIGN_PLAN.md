# DESIGN_PLAN.md — 前端审美优化设计计划

> 基于 frontend-design 技能的四问框架，在现有设计系统基础上进行审美提升。
> 
> **2026-06-01 更新**：编辑器已升级 13 项功能（自动保存/草稿恢复/禅模式/TOC/斜杠命令/快速切换/导出/代码高亮/置顶/回收站/描述/封面/图标/保存时间），新增 4 个 UI 组件（Tooltip/DropdownMenu/Toast/Toggle），所有新功能均遵循本文档的"温润书香"美学规范——stone 暖灰 + emerald 主色 + 8px 间距 + 自然呼吸动画。

---

## 一、Purpose（界面目标）

**核心问题：这个界面解决什么问题？谁在用？**

- **用户画像**：个人成长追求者 — 学生、职场人、终身学习者。他们在安静环境下记录、复盘、分享。
- **核心场景**：
  1. 深夜独自写笔记 → 需要**沉浸、无干扰**
  2. 对比版本差异 → 需要**清晰、可信任**
  3. 与 AI 教练对话 → 需要**温暖、有回应感**
  4. 浏览社区帖子 → 需要**有呼吸感、不拥挤**
- **情感目标**：让用户感觉坐在一间洒满阳光的书房里，窗外绿植摇曳。安静但不冷清，专注但不紧张。

## 二、Tone（美学风格）

**选择方向：「温润书香」(Refined Scholarly Warmth)**

灵感来源：
- 日本高端文具店（Traveler's Factory）— 纸张纹理、皮革质感、黄铜细节
- 现代独立图书馆 — 大窗采光、木质书架、绿植点缀
- Notion 的宁静感 + Linear 的精准感

具体表达：
- **主色调不变**：emerald #10b981 保持为主色（已在用户心智中建立"成长=绿色"的关联）
- **中性色调整**：从纯灰 `#6b7280` 转向暖灰 `#78716c`（stone 色系），让界面温度上升 2°C
- **背景**：`#fafaf9`（stone-50）替代 `#f3f4f6`（gray-100），更接近纸张质感
- **阴影**：多层阴影替代单层，模拟纸张叠放的真实感
- **圆角**：从 8px 提升到 12px（卡片）、6px（按钮/输入框），让边角更柔和

## 三、Constraints（技术约束）

- Tailwind CSS only，不引入新 UI 库
- 主色 emerald #10b981 不变
- 背景色系可从 gray 转向 stone（暖灰）
- 字体保持 system-ui / Inter（项目 .claude/CLAUDE.md 规定）
- 所有修改需通过自检（启动、健康检查、lint）
- 保持现有功能逻辑不变，只改 visual presentation

## 四、Differentiation（差异化记忆点）

**"呼吸的界面" — The Breathing Interface**

一个让人记住的细节：**所有动画模仿自然呼吸节奏**。

1. **卡片悬浮**：hover 时微微上浮（translateY(-4px)），阴影加深，过渡 300ms ease-out。像纸张被微风掀起一角。
2. **输入框聚焦**：emerald 光晕不是生硬的 ring-2，而是 box-shadow 扩散动画（0 → 4px blur），像水滴落入水面。
3. **页面加载**：卡片依次淡入上浮（stagger 50ms），像书页逐张落下。
4. **按钮点击**：scale(0.97) → scale(1) 弹性反馈，200ms。像轻按琴键。
5. **标签选中**：背景色从灰到 emerald 渐变动画，像墨水晕开。

**比喻**：如果普通 UI 是打印纸，这个界面是一本手工装订的软皮笔记本。

---

## 五、页面级设计指令

### 5.1 登录/注册页
- 整页居中，背景 stone-50，卡片白色、圆角 16px（rounded-2xl）
- 卡片阴影：`shadow-lg shadow-stone-200/50`
- 标题加 emerald 细线装饰（w-12 h-0.5 bg-emerald-400 mx-auto）
- 输入框聚焦：`focus:ring-0 focus:shadow-[0_0_0_3px_rgba(16,185,129,0.15)]` — 柔光晕
- 按钮 → emerald 渐变：`bg-gradient-to-r from-emerald-500 to-emerald-600`
- 链接 hover：从 underline 改为文字颜色渐变 + 底部细线动画

### 5.2 笔记列表页
- 卡片 hover：`translateY(-4px) shadow-lg`，transition 300ms
- 卡片内标签显示在底部，左对齐
- 空状态：添加柔和插画风格 emoji + 引导文案
- 新建笔记按钮：固定右下角 FAB（移动端）或顶部右侧（桌面端）

### 5.3 笔记详情页
- Tab 切换：底部指示条从 2px → 3px，加过渡动画
- 编辑区与右侧面板之间加可拖拽分隔线（resize handle）
- 版本历史列表：时间轴样式（垂直线 + 圆点），替代纯列表
- AI 对话气泡：用户气泡 emerald 渐变，AI 气泡白色带细阴影

### 5.4 社区首页
- 帖子卡片：作者头像（首字母头像）、时间相对显示（"3小时前"）
- 搜索框：聚焦时宽度微增（102% → 103%），圆角从 lg 到 xl
- 分页按钮：emerald 描边替代灰色

### 5.5 侧边栏
- 桌面端：固定左侧，w-60（保持），背景 stone-50 区分于主内容区
- 导航项：左侧 3px emerald 指示条（当前激活项）
- 品牌区：添加微妙的字母 Logo 图案
- 移动端：汉堡菜单打开时，侧边栏从左侧滑入（spring 动画感）
- 用户区：头像 + 名称 + 退出按钮，底部固定

---

## 六、配色令牌（CSS 变量扩展）

```css
:root {
  --bg-primary: #fafaf9;      /* stone-50 — 页面背景 */
  --bg-card: #ffffff;          /* 卡片背景 */
  --border: #e7e5e4;           /* stone-200 — 边框 */
  --text-primary: #1c1917;     /* stone-900 — 主文字 */
  --text-secondary: #78716c;   /* stone-500 — 辅助文字 */
  --accent: #10b981;           /* emerald-500 — 主色 */
  --accent-soft: #d1fae5;      /* emerald-100 — 柔和强调 */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.04);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.06);
  --shadow-lg: 0 12px 32px rgba(0,0,0,0.08);
}
```

---

## 七、实施优先级

| 优先级 | 范围 | 预估改动 |
|--------|------|---------|
| P0 | 全局 CSS + UI 基础组件（Button/Input/Card/Badge） | 影响所有页面 |
| P1 | 登录/注册页 | 入口体验 |
| P2 | 笔记列表 + 笔记详情 | 核心功能 |
| P3 | 社区首页 + 侧边栏 | 辅助功能 |
| P4 | 全局微交互打磨 | 锦上添花 |

